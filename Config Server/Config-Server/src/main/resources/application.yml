server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rohitgade14/MicroservicessWithConfigServer
          search-paths: Config
          clone-on-start: true
